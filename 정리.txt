1. Ctrl + Enger 계행된다 (vscode)
2. git 올리기
초기화 ->
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/kelly3131/241022private.git
git push -u origin main

   여기서부터 하면됨 !!!
   Git add .
   Git commit -m “add”
   Git push origin master

2. figma > community > dashboard > filter(free)적용
3. 이모지  Win + .
4. names details 한개만열릴때,  radio group묶을때   
5. 언제 margin과 padding을 줘야할까?

   선밖은 margin
   선안쪽은 padding

   배경색 또는 배경이미지 밖은 margin
   배경색 또는 배경이미지 안쪽은 padding

   margin을 줄때에는 반드시 한방향으로 줘야 한다
